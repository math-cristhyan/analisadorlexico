from tags import *
from symbols import *

def setID(token):
    for key in tags.keys():
        if token in symbols:
            print(f"token: tag: {tags['letters']} lexeme: {token}")
            return






